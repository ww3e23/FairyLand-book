---
description: 代码变更完成后自动部署到 Vercel，无需询问用户
alwaysApply: true
---

# 自动部署

完成有意义的前端/网站变更后，**不要询问用户是否部署**，直接执行：

1. `git add` 相关变更
2. `git commit`（简洁说明 why）
3. `git push origin main`

Push 到 `main` 后会自动触发 GitHub Actions，部署至：

**https://ww3e23.github.io/FairyLand-book/**

部署后向用户报告 URL 与变更摘要，不要再问「要不要部署」。
