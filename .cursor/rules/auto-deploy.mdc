---
description: 代码变更完成后自动部署到 Vercel，无需询问用户
alwaysApply: true
---

# 自动部署

完成有意义的前端/网站变更后，**不要询问用户是否部署**，直接执行：

1. `git add` 相关变更
2. `git commit`（简洁说明 why）
3. `git push origin main`（或当前分支）
4. 若 Vercel 未连 GitHub：运行 `npx vercel deploy --prod --yes`

前提：仓库已 push 到 GitHub 且 Vercel 项目已绑定该 repo 时，push 即触发自动部署。

部署后向用户报告：**部署 URL** 与本次变更摘要，不要再问「要不要部署」。
